import 'package:flutter/material.dart';

class AppColors {
  static const background = Color.fromRGBO(25, 26, 26, 1);
  static const sideNav = Color.fromRGBO(32, 34, 34, 1);
  static const searchBar = Color.fromRGBO(32, 34, 34, 1);
  static const searchBarBorder = Color.fromRGBO(60, 63, 64, 1);
  static const iconGrey = Color(0xFFEAEAEA); // Further lightened for better visibility
  static const textGrey = Color(0xFFEAEAEA); // Further lightened for better visibility
  static const footerGrey = Color(0xFFA9A9A9); // Further lightened for better visibility
  static const proButton = Color.fromRGBO(47, 48, 47, 1);
  static const cardColor = Color(0xFF262626);
  static const submitButton = Color.fromRGBO(27, 185, 206, 1);
  static const whiteColor = Colors.white;
  
  // New colors for better contrast on mobile
  static const darkShadow = Color(0xFF161616);
  static const lightBorder = Color(0xFF3A3A3A);
  static const activeIcon = Color(0xFF27C1D9);
}